import { Survey } from './survey';
import { DepToBranch } from './DeptToBranch';

export class Prefrredurl {
    id: string;
    survey: Survey;
    isSubmitted: boolean;
    uuid: string;
    email: string;
}